{/*        


              {foodCat.length !== 0 ? (
  foodCat.map((data) => (
    <div key={data._id} className="fs-3 m-3">
      {data.categoryName}
    </div>

{ foodItem.length !== 0 ? 
              
  // foodItem !== [] ?

   foodItem.filter((item)=>item.categoryName=== data.categoryName)
  .map(filterItems=>{
    return(
      <div key={filterItems._id} className="col-12 cold-md-6 col-lg-3">
      <Card foodName={filterItem.name}
      
      options={filterItem.options[0]}
      imgSrc={filterItem.img}
      ></Card>
      </div>
    )
  }
  ): <div>data is not  displaying</div>}
 
)

<Footer />
</div>



              
        //       { foodItem.length !== 0 ? 
              
        //       // foodItem !== [] ?

        //        foodItem.filter((item)=>item.categoryName=== data.categoryName)
        //       .map(filterItems=>{
        //         return(
        //           <div key={filterItems._id} className="col-12 cold-md-6 col-lg-3">
        //           <Card foodName={filterItem.name}
                  
        //           options={filterItem.options[0]}
        //           imgSrc={filterItem.img}
        //           ></Card>
        //           </div>
        //         )
        //       }
        //       ): <div>data is not  displaying</div>}
              
        //     }
        // </div>
   
        */}